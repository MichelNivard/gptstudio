# App Server

App Server

## Usage

``` r
mod_app_server(id, ide_colors = get_ide_theme_info())
```

## Arguments

- id:

  id of the module

- ide_colors:

  List containing the colors of the IDE theme.
